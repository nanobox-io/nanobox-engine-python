[install]
# Virtualenvs ignore distutils options that set installation paths; that is
# just what we want because allows us to always install to our custom location
# unless working in a virtualenv.
prefix={{pycache}}
